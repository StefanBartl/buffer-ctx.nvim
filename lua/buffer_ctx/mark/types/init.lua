---@module 'buffer_ctx.mark.types'
---@brief Type anchor for the mark domain.
---@description
--- Shared mark-domain types (`BufferCtx.MarkConfig`, `BufferCtx.MarkKeymaps`,
--- `BufferCtx.MarkSign`) live in `buffer_ctx.@types`. This file exists to
--- honor the per-subdirectory `/types` convention and returns an empty
--- table.

return {}
